# 📝 問題入力ヘルプ

このアプリでは、**Markdown（マークダウン）**を使ってテキストを装飾したり、**LaTeX（ラテック）**を使ってきれいな数式を表示したりすることができます。

---

## 1. テキストの装飾（Markdown）

見出しや箇条書き、強調を簡単に表現できます。
## ■ 見出し（大）
### ■ 見出し（中）

**ここを太字にします**

* 箇条書きの項目1
* 箇条書きの項目2

1. 番号付き項目1
2. 番号付き項目2

---

## 2. 数式の書き方（LaTeX）

数式を表示するときは、記号を `$` で囲みます。

* **文の中に数式を入れる場合（インライン表示）**: `$数式$` と書きます。
* 入力例：`底辺が $a$ cm、高さが $h$ cmの三角形`


* **数式を独立した行で大きく表示する場合（ブロック表示）**: `$$数式$$` と書きます。

### 📌 中学数学でよく使う数式・記号一覧

| 表示したいもの | 入力するコード | 画面での表示例 |
| --- | --- | --- |
| **かけ算 (×)** | `\times` | $x \times y$ |
| **わり算 (÷)** | `\div` | $a \div b$ |
| **分数** | `\frac{分子}{分母}` | $\frac{y}{x}$ または $\frac{1}{3}$ |
| **累乗（2乗など）** | `x^2` | $x^2$ |
| **比例・反比例・関数** | `y = ax`, `y = \frac{a}{x}` | $y = ax$, $y = \frac{a}{x}$ |
| **不等号** | `<`, `>`, `\le`, `\ge` | $x < 5$, $x \ge 3$ |
| **ルート（平方根）** | `\sqrt{数字}` | $\sqrt{2}$, $\sqrt{x}$ |
| **π（円周率）** | `\pi` | $2\pi r$ |
| **度（角度）** | `^\circ` | $60^\circ$ |
| **三角形・角** | `\triangle`, `\angle` | $\triangle ABC$, $\angle ABC$ |
| **合同・相似** | `\equiv`, `\sim` | $\triangle ABC \equiv \triangle DEF$, $\triangle ABC \sim \triangle PQR$ |
| **平行** | `\parallel` | $AB \parallel CD$ |

---

## 💡 応用・組み合わせのサンプル

### ① 方程式の文章題

### 💡 問題
ある数 $x$ を 3 倍して 5 を足した数は、もとの数を 5 倍して 3 引いた数と等しくなります。
このとき、方程式 $$3x + 5 = 5x - 3$$ を解いて、ある数 $x$ を求めなさい。


### ② 図形・関数の問題

### 💡 問題
下図において、$\triangle ABC$ は $\angle B = 90^\circ$ の直角三角形です。
$AB = 6\text{cm}$, $BC = 8\text{cm}$ のとき、辺 $AC$ の長さを求めなさい。

（ヒント：三平方の定理 $a^2 + b^2 = c^2$ を使いましょう）

---

## ⚠️ 入力時の注意点

* `\frac` などの命令（コマンド）の直後にアルファベットを続ける場合は、`\frac{1}{2}x` のようにスペースを空けるか、波括弧 `{}` で区切ってください。
* 記号やコマンド（`$` や `\`、`{}` など）は、**すべて半角**で入力してください。
